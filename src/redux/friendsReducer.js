const initialState = [
    { id: 1, nickname: "mark_orlov" },
    { id: 2, nickname: "sara_li" },
    { id: 3, nickname: "oleg_lsp" },
    { id: 4, nickname: "andabura" },
];

const friendsReducer = (state = initialState, action) => {
    return state;
};

export default friendsReducer;
